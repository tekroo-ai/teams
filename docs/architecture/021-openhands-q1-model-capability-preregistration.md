# OpenHands-Q1 model-capability preregistration

## Status

`FROZEN_CANDIDATE — NOT ACCEPTED — EXECUTION NOT AUTHORIZED`

## Purpose

This preregistration turns the approved model-capability remediation into a
finite OpenHands investigation. Its normative candidate is
`investigations/openhands-q1/preregistration.json`.

The investigation qualifies exact deployable profiles, not model families or
marketing names. It tests identity, instruction and tool fidelity, qualified
assignment, rejection, bounded execution, promotion, failover, cancellation,
restart fencing, independent review, N-version isolation, provenance, secrets,
SMA's non-authoritative boundary, finite budgets, and terminal outcomes.

## Authority boundary

OpenHands, model, provider, tool, and SMA outputs remain evidence. Only Teams'
deterministic kernel may authorize transitions or finalize organizational
state. A textual completion claim cannot satisfy deterministic or independent
review gates.

## Candidate and preexecution boundary

The candidate deliberately contains unbound source, runtime, endpoint, prompt,
tool, model-profile, credential, isolation, and optional SMA identities. Those
are not omissions that execution may fill informally. A separate immutable
preexecution package must bind every placeholder, pass the fake-profile path,
and receive principal acceptance and live-execution authority.

This document and its candidate execute no OpenHands, provider, model, Ollama,
or SMA operation and authorize no expenditure or production activity.
