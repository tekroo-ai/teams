# SMA-S2 final-P2 closure T2 readiness

Date: 2026-08-28
Status: `PASS_T2_UNPUBLISHED_AWAITING_T3_INDEPENDENT_REVIEW`

This revision supersedes the first T2 package rejected at T3. The rejected
hashes remain historical evidence; none is reused as the current identity.

This record advances the authorized definitive closure plan through T1 and T2
only. It is not a candidate, execution identity, live authority, or scientific
adjudication.

## OBSERVED bindings

- Frozen T0 product-truth manifest:
  `c2520df45c0ac318fc3d23cef864586b609032179eae6a59c955898758bb476e`.
- Frozen T0 independent review:
  `f622ffdf898fdb9a0a426208bec816724771de010ac3d96a4ff1f1d414758eb3`.
- Mutable T1 tree identity:
  `7dd43921357a3ad81d190ddd75228cfd6cfd2099439448c6c35a8f87c1e92adb`.
- Mutable package manifest SHA-256:
  `58348d41fca6586eb1ed7a921f624abe558e1f3fe669a3752c57fb729baa9207`.
- Traceability SHA-256:
  `ce87f34b1f32f9dfa0cafb5460961b42ad70e90a99660fe2799bbd6ff1a9a0a0`.
- T2 qualification file SHA-256:
  `4bed960ffa74b96fce247c6faddc58c0b06ae958fc6be64f453eebf93f5ceaef`.
- T2 receipt content identity:
  `3dae7b85a1674630649832ca0ea08c577c2dbd0364b83e661360ba8769fe4825`.
- Frozen non-live launch definition SHA-256:
  `1f1b3597dcb8e3c472d8955bcf344d1dda45bb81100976d6c3af8bea8dc8040a`.

## COMPUTED qualification result

- Integrated zero-credit selector: `34/34 PASS`.
- Integrated measured plan: `103/103 PASS`.
- Scientific targeted raw-evidence mutation controls: `59/59 rejected`; each
  control changes one named retained path and preserves the remaining evidence.
- Evidence classes: `10/10` positive, `10/10` missing rejected, and `10/10`
  present-but-mutated rejected.
- Product-surface conformance controls: `33/33 PASS`.
- Dress and measured append-before-action ledgers: valid.
- Independent in-package case-oriented reconstruction, with no primary-oracle
  import: `34/34` and `103/103`.
- Normal and optimized Python complete qualification outcomes: equivalent.

The successor also enforces the observed OpenHands `SettingsResponse` envelope,
HTTP 201 conversation creation, HTTP 200 deletion, exact stub record/case/
repetition/mode identity, whole-operation deadlines, exact JSONL cardinality,
stable positive and negative reads, and stabilized `ConversationInfo` terminal
states. Stub evidence and readiness state are reset per operation. Consistent
seeded memories are promoted through the bound product replay path. Exact-event
reconciliation now requires two successful exact-event scans receipted by a
bound transparent OpenHands capture proxy and verifies stable exact
cardinality. Case 14 uses the bound client-tool surface. Case 19's
transport-failure endpoint is frozen and held by a bound non-listening port
guard, preventing either an unrelated service or a real model call.
Case 20 submits in the background, interrupts active work, then performs and
receipts `stop_stub`, `stop_bridge`, `stop_sma`, and owned inventory before
cleanup.

The package now contains the default-deny live composition, concrete disposable
process-action implementation, exact loopback endpoints/namespaces/fixture
files, eleven bound file hashes—including the transitive stub dependency and
the manifest-bound seed truth—and the authority-consumption chain. T2
validated this surface without constructing live ports or starting anything.

The runner decodes the deterministic stub's bound raw JSONL request body,
queries MongoDB by nested OpenHands provenance while retaining `_id`, joins
retrievals by `task_ref`, checks Qdrant by `memory_id + agent_id`, and derives
the episodic point identity from the Mongo memory ID. Cleanup PASS comes from a
raw process/workspace/Mongo/Qdrant inventory receipt rather than an action exit
code.

## Authorization boundary

`NOT_RUN`: service start, live preflight, OpenHands conversation, live dress,
measured execution, real-model call, M1, E1, production/historical access,
commit, and push.

The next and only active gate is T3 independent readiness review. H0 publication
is prohibited unless T3 returns exact `PASS_READY_TO_PUBLISH` against this
stable artifact set.
